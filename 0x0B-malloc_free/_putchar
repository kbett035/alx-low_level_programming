#include "main.h"
#include <unistd.h>
/**
 * _putchar -it  writes the character c to stdout
 * @c: The char to print
 *
 * Return: On success 1.
 * On error, -1 will be returned, and erroro is set appropriately.
 */
int _putchar(char c)
{
	return (write(1, &c, 1));
}
